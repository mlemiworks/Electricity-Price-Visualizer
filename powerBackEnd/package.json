{
  "name": "powerbackend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd .. && cd powerReactApp && npm run build && cp -r dist ../powerBackEnd",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"$MSG\" && git push origin main"
  },
  "author": "Markus Lemiläinen",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "moment-timezone": "^0.6.2",
    "node-cache": "^5.1.2",
    "node-cron": "^3.0.3",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
